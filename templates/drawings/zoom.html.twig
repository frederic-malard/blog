{% extends 'base.html.twig' %}

{% block title %}Dessins{% endblock %}

{% block body %}

<div class="container jumbotron my-1 py-1">
    <h2 class="my-2 text-center">
        {{drawing.nom}}
        <span class="ml-2">-</span>
        <a href="{{path('drawing_show', {'slug' : drawing.slug})}}" class="btn btn-primary ml-2">
            Retour
        </a>
    </h2>
</div>
<p>
    <img src="{{drawing.url}}" alt="dessin">
</p>

{% endblock %}
